---
年度: 平成30年度
時期: 秋期
区分: 午前
問題番号: 問40
カテゴリ: テクノロジ系
難易度: ★★
重要度: ★
タグ: 技術要素, VPN・トンネリング, 最優先
---

# 平成30年度秋期 問40（技術要素）

## 問題文

インターネットに接続された利用者のPCから，DMZ上の公開Webサイトにアクセスし，利用者の個人情報を入力すると，その個人情報が内部ネットワークのデータベース（DB）サーバに蓄積されるシステムがある。このシステムにおいて，利用者個人のディジタル証明書を用いたTLS通信を行うことによって期待できるセキュリティ上の効果はどれか。

ア　PCとDBサーバ間の通信データを暗号化するとともに，正当なDBサーバであるかを検証することができるようになる。

イ　PCとDBサーバ間の通信データを暗号化するとともに，利用者を認証することができるようになる。

ウ　PCとWebサーバ間の通信データを暗号化するとともに，正当なDBサーバであるかを検証することができるようになる。

エ　PCとWebサーバ間の通信データを暗号化するとともに，利用者を認証することができるようになる。

## 使用画像

なし

## 解答と解説

**正解：エ**

このシステムでは、利用者のPCはDMZ上の公開WebサーバとTLS通信を行っており、DBサーバとは直接通信していない（DBサーバは内部ネットワークにあり、Webサーバ経由でアクセスされる）。したがって、利用者個人のディジタル証明書（クライアント証明書）を用いたTLSクライアント認証を行うことで期待できる効果は、PCとWebサーバ間の通信データの暗号化に加え、証明書に基づき利用者本人であることをサーバ側が認証（クライアント認証）できるようになることである。

- ア・イ：通信区間を「PCとDBサーバ間」としている点が誤り。PCが直接TLS通信するのはWebサーバであり、DBサーバとは通信しない。
- ウ：「PCとWebサーバ間」の暗号化は正しいが、「正当なDBサーバであるかを検証する」という効果は、利用者側のクライアント証明書がもたらすものではない（それはサーバ証明書によるサーバ認証の役割であり、逆方向の効果を混同している）。

**IPA公式：エ**
