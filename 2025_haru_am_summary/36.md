---
年度: 令和7年度
時期: 春期
区分: 午前
問題番号: 問36
カテゴリ: テクノロジ系
難易度: ★★★
重要度: ★★
タグ: 技術要素, メール・Web通信, Webセキュリティ, レスポンシブWeb
---

# 令和7年度春期 問36（技術要素）

## 問題文

Webサーバのログを分析したところ，Webサーバへの攻撃と思われるHTTPリクエストヘッダーが記録されていた。次のHTTPリクエストヘッダーから推測できる，攻撃者が悪用しようとしていた可能性が高い脆（ぜい）弱性はどれか。ここで，HTTPリクエストヘッダー中の“%20”は空白を意味する。

〔HTTPリクエストヘッダーの一部〕

　  GET /cgi-bin/submit.cgi?user=;cat%20/etc/passwd HTTP/1.1

　  Accept: */*

　  Accept-Language: ja

　  UA-CPU: x86

　  Accept-Encoding: gzip, deflate

　  User-Agent: （省略）

　  Host: test.example.com

　  Connection: Keep-Alive

ア　HTTPヘッダーインジェクション（HTTP Response Splitting）

イ　OSコマンドインジェクション

ウ　SQLインジェクション

エ　クロスサイトスクリプティング

## 使用画像

なし

## 解答と解説

**正解：イ**

リクエストの中の`user=;cat%20/etc/passwd`という部分に注目する。“;”はシェルにおいてコマンドを連結・区切る記号であり、`cat /etc/passwd`はUNIX/Linux系OSでパスワード関連情報を含むファイルを表示するOSコマンドである。すなわちこの入力は、CGIスクリプトがuserパラメータの値をOSのシェルコマンドとして実行してしまう脆弱性を突き、任意のOSコマンド（ここでは`cat /etc/passwd`）を実行させようとするOSコマンドインジェクション攻撃を意図したものである。

他の選択肢は誤り。アのHTTPヘッダーインジェクションは改行コードなどをリクエスト/レスポンスヘッダーに注入する攻撃、ウのSQLインジェクションはSQL文の構文を悪用してデータベースを不正操作する攻撃（`'`や`OR 1=1`等が典型）、エのクロスサイトスクリプティングは`<script>`タグなどを注入してブラウザ上で悪意のあるスクリプトを実行させる攻撃であり、いずれも本設問のようなOSコマンド実行を狙った文字列とは特徴が異なる。

**IPA公式：イ**
