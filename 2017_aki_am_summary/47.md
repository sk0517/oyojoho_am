---
年度: 平成29年度
時期: 秋期
区分: 午前
問題番号: 問47
カテゴリ: テクノロジ系
難易度: ★★
重要度: ★★
タグ: 開発技術, 適切なものを選ぶ
---

# 平成29年度秋期 問47（開発技術）

## 問題文

テストで使用されるスタブ又はドライバの説明のうち，適切なものはどれか。

ア　スタブは，テスト対象モジュールからの戻り値を表示・印刷する。

イ　スタブは，テスト対象モジュールを呼び出すモジュールである。

ウ　ドライバは，テスト対象モジュールから呼び出されるモジュールである。

エ　ドライバは，引数を渡してテスト対象モジュールを呼び出す。

## 使用画像

なし

## 解答と解説

**正解：エ**

トップダウンテストでは、上位モジュールから下位モジュールへと結合していくため、未完成の下位モジュールの代わりに「スタブ」を用いる。スタブは、テスト対象モジュールから呼び出され、戻り値などを返すダミーモジュールである。
一方、ボトムアップテストでは、下位モジュールから上位モジュールへと結合していくため、未完成の上位モジュールの代わりに「ドライバ」を用いる。ドライバは、テスト対象モジュールに引数を渡して呼び出す側のダミーモジュールである。したがって選択肢エが正しい。

- ア：スタブの説明として不正確（戻り値の表示・印刷ではなく、呼び出されて疑似的な戻り値を返すもの）。
- イ：スタブとドライバの役割が逆。呼び出す側はドライバである。
- ウ：ドライバとスタブの役割が逆。呼び出される側はスタブである。

**IPA公式：エ**
