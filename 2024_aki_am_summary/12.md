---
年度: 令和6年度
時期: 秋期
区分: 午前
問題番号: 問12
カテゴリ: テクノロジ系
難易度: ★★
重要度: ★★
タグ: コンピュータシステム, 適切なものを選ぶ, 冗長化・フェールオーバー
---

# 令和6年度秋期 問12（コンピュータシステム）

## 問題文

Webアプリケーションサーバの信頼性に関する記述のうち，適切なものはどれか。

ア　コールドスタンバイ構成で稼働しているサーバに障害が発生した場合，サービスは中断しないが，トランザクションは継続できない。

イ　コールドスタンバイ構成で稼働しているサーバに障害が発生した場合，サービスは中断するが，トランザクションは継続できる。

ウ　セッションを共有しないクラスタ構成で1台のサーバに障害が発生した場合，サービスは継続できないが，トランザクションは継続できる。

エ　セッションを共有するクラスタ構成で1台のサーバに障害が発生した場合，サービス及びトランザクションは継続できる。

## 使用画像

なし

## 解答と解説

**正解：エ**

セッションを共有するクラスタ構成では、各サーバがセッション情報（クライアントとの通信状態やトランザクションの進行状況など）を共有・複製している。そのため、1台のサーバに障害が発生しても、他のサーバがセッション情報を引き継いで処理を継続できるため、サービスもトランザクションも中断せずに継続できる。これがエの記述である。

- ア・イ：コールドスタンバイ構成は、障害発生後に待機系サーバを起動してデータやセッションを復元するため、その間サービスは中断し、進行中のトランザクションも継続できない。両方とも誤り。
- ウ：セッションを共有しないクラスタ構成では、障害が発生したサーバが保持していたセッション情報が失われるため、サービス・トランザクションともに継続できない。「トランザクションは継続できる」は誤り。

以上より、正解はエである。

**IPA公式：エ**
